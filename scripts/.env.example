# scripts/.env로 복사해서 실제 값을 채운다 (scripts/.env는 git에 커밋되지 않음).
# 값은 GitLab Settings > CI/CD > Variables(DEV_LITELLM_MASTER_KEY 등) 또는
# Cloud Run 콘솔의 서비스 환경변수에서 확인한다.

LITELLM_BASE_URL=http://fab0704-api.dev.gcp.lgchem.com/
LITELLM_MASTER_KEY=<LITELLM_MASTER_KEY 값을 채워넣으세요>
